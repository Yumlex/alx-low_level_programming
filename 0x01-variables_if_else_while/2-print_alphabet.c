#include <stdio.h>

/**
 * main - Prints the alpabet.
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char alp[26] = "abcdefghijklmnopqrstuvwwyz" int i;

	for(i = 0; < 26; i++)
	{
		putchar(alp[i]);
	}
	putchar('\n');
	return (0);
}
